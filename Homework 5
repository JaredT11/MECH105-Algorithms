###### *Matrices*

function [A] = specialMatrix(n,m)

A = zeros(n,m);

A (1,:) = 1:m

A (:,1) = 1:n

for k=2:n

for j=2:m

A(k,j)=A(k-1,j)+A(k,j-1);

end

if nargin ~=2

error('need more inputs')

end

end
